# Cursor IDE: agent rules and local IDE config (not shared via git)
.cursor/

/target
/result
/result-*
/result-kctl
/dist
# Optional local release notes override for break-glass `make release-publish`
/RELEASE_NOTES.md
*.iso
